# AAD Binds Schema, Key, and Algorithm

V1 authenticated data binds the schema, Recipient Key ID, and declared algorithm identifiers for Keyring wrapping and Data Envelopes. Display metadata such as creation time is not part of AAD, so formatting or migration of non-security metadata does not affect decryption compatibility.
