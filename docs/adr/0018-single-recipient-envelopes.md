# Single-recipient Envelopes

V1 Data Envelopes target exactly one Recipient Key ID. Multi-recipient envelopes would require recipient stanzas and additional permission semantics, so team sharing or multi-user access is deferred to a future schema instead of complicating the first stable format.
