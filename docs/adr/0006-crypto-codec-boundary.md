# Crypto Codec Boundary

HX-DataLock is a Crypto Codec, not a storage, network, or synchronization SDK. The library converts Payload Bytes into Data Envelopes and back; repositories, databases, cloud drives, local caches, business serialization, and conflict handling remain application responsibilities.
